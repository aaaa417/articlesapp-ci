name: CI ArticlesApp

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Récupérer le code
        uses: actions/checkout@v4

      - name: 🟢 Configurer Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: 📦 Installer les dépendances
        run: npm install

      - name: 🔍 Vérifier la qualité (Linter)
        run: npm run lint || echo "Linter a trouvé des erreurs, mais on continue"

      - name: 🧪 Lancer les tests
        run: npm test