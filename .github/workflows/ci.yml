name: CI ArticlesApp

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4 # Version 4 est plus stable

      - name: 🟢 Setup Node
        uses: actions/setup-node@v4 # Version 4
        with:
          node-version: 18
          cache: 'npm' # Ajoute du cache pour accélérer l'installation

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔍 Run Linter
        run: npm run lint || echo "Linter failed but continuing..."

      - name: 🧪 Run Tests
        run: npm test